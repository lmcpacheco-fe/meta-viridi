/* Possible reference:
https://community.nxp.com/t5/i-MX-Processors/GPIO-Wake-up/m-p/1832148#M221532
*/
&gpio3 {
	gpio-keys {
		compatible = "gpio-keys";
		pinctrl-0 = <&pinctrl_wakeup>;
		pinctrl-names = "default";

		wakeup {
			label = "WAKEUP PINS";
			gpios = <&gpio4 2 GPIO_ACTIVE_LOW>,
				<&gpio4 7 GPIO_ACTIVE_LOW>,
				<&gpio4 16 GPIO_ACTIVE_LOW>,
				<&gpio4 21 GPIO_ACTIVE_LOW>;
			linux,code = "KEY_WAKEUP";
			debounce-interval = <10>;
			wakeup-source;
		};
	};
};

&iomuxc {
	pinctrl_wakeup: wakeupgrp {
		// Configured to be pullups, as the signal should be active low
		fsl,pins = <
			MX91_PAD_ENET1_TD3__GPIO4_IO2		0x31e
			MX91_PAD_ENET1_TXC__GPIO4_IO7		0x31e
			MX91_PAD_ENET2_TD3__GPIO4_IO16		0x31e // was 0x51e (a pulldown)
			MX91_PAD_ENET2_TXC__GPIO4_IO21		0x31e // was 0x51e (a pulldown)
		>;
	};
};
